<div class="ranking">
    <h1>Haveランキング</h1>
    <div class="container">
        <% @ranking.each do |item, rank| %>
            <%= render 'item', item: item, rank: rank, type: "Have" %>
        <% end %>
    </div>
</div>
