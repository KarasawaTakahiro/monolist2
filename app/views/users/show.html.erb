<% content_for :head do %>
  <title><%= @user.name + ' - monolist' %></title>
  <%= tag "meta", {name: 'description', content: @user.items.collect{|item| item.title}.join(' ')} %>
<% end %>

<%= render 'profile' , user: @user %>


<%= render 'items/list' , items: @items %>